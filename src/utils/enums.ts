export enum NotiFunc {
    FOLLOW = "follow",
    TREE = "tree",
    COMMENT = "comment",
    LIKE = "like"
}

export enum Role {
    ADMIN = "admin",
    USER = "user",
    BOSS = "boss"
}

export enum Plan {
    FREE = "free",
    PRO = "pro",
    PREMIUM = "premium"
}

export enum TreeCriteria {
    CREATED_AT = "createdAt",
    UPDATED_AT = "updatedAt",
    LIKES = "likes",
    COMMENTS = "comments",
    NAME = "name"
}

export enum Order {
    ASC = "asc",
    DESC = "desc"
}